[package]
name = "manic-miner-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustzx-core = { version = "0.16.0", features = ["embedded-roms", "sound"] }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
